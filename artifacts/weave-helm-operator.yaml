---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: weave-flux-helm-operator
  labels:
    name: weave-flux-helm-operator
    app: weave-flux
    weave-cloud-component: flux
    weave-flux-component: helm-operator
  namespace: weave
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        name: weave-flux-helm-operator
        app: weave-flux
        weave-cloud-component: flux
        weave-flux-component: helm-operator
    spec:
      volumes:
      - name: git-key
        secret:
          secretName: flux-git-deploy
          defaultMode: 0400
      serviceAccountName: weave-flux
      containers:
      - name: weave-flux-helm-operator
        image: 'quay.io/tamara/helm-operator:alpha'
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - name: git-key
          mountPath: /etc/fluxd/ssh
          readOnly: true
        args:
        # replace (at least) the following URL
        - '--git-url=git@github.com:tamarakaufler/flux-helm-test.git'
        - '--git-branch=v1alpha2-format'
        - '--git-charts-path=charts'
